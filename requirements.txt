# Dependencies for the Streamlit app (app/app.py).
#
# Streamlit Community Cloud installs from this file, at the repository root,
# so it holds ONLY what the app imports. The modelling stack - torch, chronos,
# torch-geometric - lives in requirements-notebooks.txt and must not be here:
# it would blow the Cloud build limits for a dashboard that reads parquet.
streamlit>=1.40
pandas>=2.1
numpy>=1.26
plotly>=5.20
pyarrow>=15.0
